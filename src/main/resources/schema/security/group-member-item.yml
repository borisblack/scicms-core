coreVersion: v1
kind: Item
includeTemplates:
  - defaultItemTemplate
metadata:
  name: groupMember
  tableName: sec_group_members
  displayName: Group Member
  singularName: groupMember
  pluralName: groupMembers
  description: Group Members
  core: true
  performDdl: false
spec:
  attributes:
    group:
      type: relation
      relType: manyToOne
      target: group
      inversedBy: roles
      columnName: group_id
      displayName: Group
      description: Group
    user:
      type: relation
      relType: manyToOne
      target: user(username)
      columnName: username
      displayName: User
      description: User
  indexes:
    sec_group_members_uk:
      columns:
        - group
        - user
      unique: true
