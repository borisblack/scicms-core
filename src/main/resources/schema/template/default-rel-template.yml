coreVersion: v1
kind: ItemTemplate
metadata:
  name: defaultRelTemplate
spec:
  attributes:
    sortOrder:
      type: int
      columnName: sort_order
      displayName: Sort Order
      description: Sort Order