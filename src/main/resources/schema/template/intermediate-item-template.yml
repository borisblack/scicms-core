coreVersion: v1
kind: ItemTemplate
metadata:
  name: intermediateItemTemplate
spec:
  attributes:
    sortOrder:
      type: int
      columnName: sort_order
      displayName: Sort Order
      description: Sort Order
      colWidth: 180
      fieldWidth: 100