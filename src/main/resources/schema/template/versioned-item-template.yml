coreVersion: v1
kind: ItemTemplate
metadata:
  name: versioned
  pluralName: versioned
  core: true
spec:
  indexes:
    _major_rev_idx:
      columns:
        - major_rev
    _is_current_idx:
      columns:
        - is_current
