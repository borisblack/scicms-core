coreVersion: v1
kind: Item
includeTemplates:
  - defaultItemTemplate
  - defaultRelTemplate
metadata:
  name: allowedPermission
  dataSource: main
  tableName: sec_allowed_permissions
  displayName: Allowed Permission
  singularName: allowedPermission
  pluralName: access
  description: Allowed Permissions
  core: true
  performDdl: true
  versioned: false
spec:
  properties:
    source:
      type: relation
      columnName: source_id
      target: item
      relType: oneToOne
      displayName: Source Item
      description: Source Item
    target:
      type: relation
      columnName: target_id
      target: permission
      relType: oneToOne
      displayName: Target Permission
      description: Target Permission
  indexes:
    sec_allowed_permission_source_target_uk:
      columns:
        - source
        - target
      unique: true
