coreVersion: v1
kind: Item
includeTemplates:
  - defaultItemTemplate
metadata:
  name: permission
  tableName: sec_permissions
  displayName: Permission
  singularName: permission
  pluralName: permissions
  description: Permission
  core: true
  performDdl: false
spec:
  properties:
    name:
      type: string
      columnName: name
      displayName: Name
      description: Name
      required: true
      length: 50
    identities:
      type: relation
      target: identity
      relType: manyToMany
      relItem: access
      inversedBy: permissions
      displayName: Identities
      description: Identities
  indexes:
    sec_permissions_name_uk:
      columns:
        - name
      unique: true
